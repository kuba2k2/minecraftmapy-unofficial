package pl.szczodrzynski.minecraftmapy.model.enum

enum class SortMode(val id: Int) {
    BEST_MATCH(0),
    NEWEST(1),
    WORTH_SEEING(2),
    BEST_RATED(3),
    MOST_DOWNLOADED(4),
    OLDEST(5)
}
